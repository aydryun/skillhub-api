name: Docker Image CI

on:
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
    environment: main

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . -t skillhub-api-release:${{ github.sha }}
